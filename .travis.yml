language: java
jdk:
  - oraclejdk8

sudo: false
install: true

script:
  - cd webapp
  #- ./webapp/gradlew build
  - ./gradlew assemble

notifications:
  username:
    recipients:
      - gu.ju@husky.neu.edu
    on_success: always 
    on_failure: always # default: always
